# Ledger de la loop d'ingénierie autonome (loop-engineering)

_Append-only. Une entrée par exécution du skill `.claude/skills/loop-engineering/SKILL.md`.
Jamais réécrit — nouvelle entrée ajoutée en dessous. Voir `project-docs/loop-policy.md`._

_⚠ Chaque entrée est un instantané figé de ce qui était vrai **au moment de ce run
précis** (bug non corrigé, blocage encore présent, etc.) — pas une trace vivante mise
à jour. Un blocage mentionné dans une entrée ancienne peut être résolu depuis. Pour
l'état **actuel** d'un bug ou d'une tâche, se référer à `project-docs/bugs.md` et
`project-docs/todo.md`, jamais à ce fichier seul._

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche de tête : faille isolation `GET /api/tickets/:id`

- Tâche : « Corriger `GET /api/tickets/:id` (`src/routes/tickets.ts:160`) avec le patron `getBoutiqueId(...)` + `403` » — `project-docs/todo.md:5` (section 🔴 PRIORITÉ CRITIQUE). Tête de file selon l'ordre déterministe de `pick-task.mjs` (priorité 🔴 d'abord).
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code.
- Risque : **élevé** (isolation multi-tenant — `boutique_id` / `getBoutiqueId`). La tâche est elle-même documentée dans `todo.md` comme « classé risque élevé par `loop-policy.md` — escaladé, pas d'auto-fix ». `loop-policy.md` § Classification interdit l'auto-commit sur cette catégorie.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — voir Détail).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Blocage environnement (bloquant, indépendant du choix de tâche)** : cette session est non-interactive et l'exécution de `node`/`npm`/`npx` est soumise à approbation interactive impossible à accorder ici. `scripts/loop/check-quota.mjs` (Étape 0bis, gate quota obligatoire *avant tout le reste*) n'a pas pu être exécuté — testé via Bash et PowerShell, avec et sans sandbox, systématiquement refusé. Les gates de l'Étape 5 (`vitest`, `tsc --noEmit`, `npm run build`, Playwright) sont tous des invocations `node`/`npm` et sont donc également inexécutables. La machinerie de sûreté de la loop ne peut pas tourner → per garde-fou global « en cas de doute non couvert → escalader », arrêt sans implémentation.
  2. **Blocage classification (bloquant, indépendant du blocage environnement)** : la tâche de tête est à risque élevé (isolation multi-tenant) et déjà escaladée sans décision humaine actée depuis (aucune entrée `decisions.md` ni changement du texte de la tâche). `loop-policy.md` interdit l'auto-fix ; l'Étape 1 impose de ne jamais re-tenter en boucle une telle tâche.
  - Recommandation : (a) pour lever le blocage environnement, lancer la loop via `scripts/loop/run-loop.ps1` dans une session CLI locale normale (permissions node/npm accordées), pas dans une session non-interactive restreinte ; (b) pour la faille isolation elle-même, c'est une correction de sécurité qui nécessite une décision humaine explicite (voir `bugs.md` § faille `GET /api/tickets/:id`) — appliquer manuellement le patron `getBoutiqueId(user, queryBoutiqueId)` + `ticket.boutique_id !== boutiqueId → 403` déjà utilisé sur `/api/tickets/:id/photos`, puis auditer `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`, `POST /:id/acompte`.

## Run 2026-07-19 (heure exacte indisponible — non-interactif) — sélecteur de tâches cassé (faux « backlog vide »)

- Tâche : **aucune tâche sélectionnée** — `pick-task.mjs` retourne `{"empty":true}` ET `--all` retourne `[]`, alors que `project-docs/todo.md` contient **54 tâches non cochées** (`grep -c '- [ ]'`), dont la section 🔴 PRIORITÉ CRITIQUE et plusieurs items 🔴.
- Issue : **escaladé** — aucun commit, aucun worktree, aucune modification de code. Le run n'a PAS traité de tâche produit, mais le résultat « empty » est un **faux positif** (bug d'outillage), pas un vrai backlog vide → ne pas appliquer la branche « empty → stop » du SKILL comme si le backlog était réellement épuisé.
- Risque : **faible** (le correctif proposé touche `scripts/loop/pick-task.mjs`, outillage de la loop — hors catégories de risque produit de `loop-policy.md` : ni auth, ni multi-tenant, ni NF525, ni RGPD, ni migration).
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — arrêt à l'étape 1, sélection impossible).
- Worktree : aucun (arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Cause racine confirmée** : `git ls-files --eol` montre le working tree en **CRLF** (`w/crlf`) pour `project-docs/todo.md`, `docs/TODO.md` ET `scripts/loop/pick-task.mjs` (checkout Windows, `core.autocrlf`, aucun `.gitattributes`). `pick-task.mjs:40` fait `readFileSync(relPath,'utf8').split('\n')` : chaque ligne conserve un `\r` traînant. Les regex `^(#{2,4})\s+(.*)$` (titres) et `^(\s*)-\s*\[ \]\s+(.*)$` (tâches) échouent car `.` ne consomme pas `\r` et `$` (sans flag `m`) ne matche pas avant `\r`. → 0 tâche parsée sur Windows, à chaque run. La loop se croit à vide et ne fait jamais rien.
  2. **Impact** : sur toute machine Windows (le poste principal de l'utilisateur, cf. CLAUDE.md), la loop-engineering est **silencieusement inopérante** — elle rapporte « backlog vide » alors que le backlog est plein (54 tâches, dont des 🔴). Un run précédent a d'ailleurs pu masquer cet état.
  3. **Correctif proposé (non appliqué — attente validation utilisateur, règle « proposer avant de modifier »)** : `pick-task.mjs:40` remplacer `.split('\n')` par `.split(/\r?\n/)`. Une ligne, robuste LF et CRLF. Vérification suggérée : `node scripts/loop/pick-task.mjs --all` doit alors lister les 54 tâches (tête de file = la 🔴 critique restante).
  4. Ce correctif étant un changement d'outillage à faible risque, il serait éligible à l'auto-commit de la loop une fois validé — mais il conditionne la loop elle-même, d'où l'escalade explicite plutôt qu'un auto-fix silencieux.

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche `cac47a6824` : audit isolation `PUT /:id` · `PUT /:id/statut` · `DELETE /:id` · `POST /:id/acompte`

- Tâche : « Auditer dans la foulée `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`, `POST /:id/acompte` (même service, même risque potentiel, pas encore vérifiés un par un) » — `project-docs/todo.md:6` (section 🔴 PRIORITÉ CRITIQUE, suite du fix `GET /api/tickets/:id`). Tête de file selon `pick-task.mjs`. Première sélection de CETTE tâche (le run précédent avait escaladé la tâche sœur `GET /:id`, désormais corrigée `ae6795f`) → pas de `--skip`, traitée normalement.
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — double catégorie `loop-policy.md` : isolation multi-tenant (`boutique_id` / `getBoutiqueId`) **ET** paiement / acompte. `loop-policy.md` § Classification interdit l'auto-commit sur ces catégories ; § Protocole d'escalade impose un rapport écrit.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation : **audit statique read-only effectué (sans modifier le code) pour rendre l'escalade actionnable.** Résultat — **3 des 4 endpoints sont vulnérables**, même classe que la faille `GET /:id` corrigée le même jour :
  1. **`POST /api/tickets/:id/acompte`** (`src/routes/tickets.ts:575`) → **SÛR**. Contient déjà `getTicketById` + `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403` (lignes 581-583). Aucune action.
  2. **`PUT /api/tickets/:id`** (`src/routes/tickets.ts:259`) → **VULNÉRABLE**. Aucun garde d'isolation ; passe `user.sub` seul à `updateTicket` (`src/services/ticketService.ts:552`) dont le `SELECT`/`UPDATE` filtre uniquement par `id` (aucun `boutique_id`). N'importe quel compte authentifié peut modifier un ticket d'une autre boutique (diagnostic, prix, notes, signature…) en itérant sur l'ID.
  3. **`PUT /api/tickets/:id/statut`** (`src/routes/tickets.ts:287`) → **VULNÉRABLE**. Idem : `updateStatutTicket` (`ticketService.ts:625`) filtre par `id` seul. Cross-boutique en écriture, et déclenche en plus la création de garantie + emails client (`sendTicketTermine`/`sendTicketLivre`) sur un ticket d'une autre boutique.
  4. **`DELETE /api/tickets/:id`** (`src/routes/tickets.ts:352`) → **VULNÉRABLE**. `requireRole('admin','manager')` limite le rôle mais **pas la boutique** ; `deleteTicket` (`ticketService.ts:688`) fait un soft-delete filtré par `id` seul. Un admin/manager d'une boutique peut soft-delete le ticket d'une autre boutique.
  - **Recommandation** : correction de sécurité à faire **manuellement par l'utilisateur** (décision humaine explicite, hors périmètre auto-fix de la loop). Deux patrons possibles, à trancher côté humain : (A) réappliquer dans chaque route le garde déjà utilisé sur `GET /:id`, `/:id/photos` et `/:id/acompte` (`getTicketById`/`getTicketForPhoto` → `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403`) avant d'appeler le service ; **ou** (B) durcir la couche service (`updateTicket`/`updateStatutTicket`/`deleteTicket`) pour recevoir et filtrer par `boutique_id` (défense en profondeur, cohérent avec l'invariant « ne jamais supposer qu'un filtre en amont suffit » de `CLAUDE.md`). Le patron (A) est le plus proche du fix `ae6795f` déjà validé en prod. Après correction : couvrir les 3 endpoints par `tests/e2e/isolation.spec.ts` (même gate qui a révélé la faille `GET /:id`) et documenter dans `bugs.md`.

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche `61fc30924e` : cache-busting par hash de contenu (config Vite)

- Tâche : « Configurer Vite pour hasher les assets `public/static/js/*.js`/`*.css` (actuellement copiés tels quels, hors du pipeline de build Vite qui gère déjà le hachage pour d'autres assets) » — `project-docs/todo.md:13` (section 🔴 PRIORITÉ prochaine session — Cache-busting par hash de contenu). Sélectionnée après `--skip cac47a6824` (tâche de tête `cac47a6824` audit isolation `PUT/DELETE/acompte` : déjà escaladée au run précédent, risque élevé, aucune décision humaine actée depuis → passée par `pick-task.mjs --skip` conformément à l'Étape 1 du SKILL, non re-tentée).
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — deux déclencheurs indépendants de `loop-policy.md` § Classification : (1) **Périmètre large / architectural** — changement de plus de ~8 fichiers ; (2) zone cache-busting historiquement fragile (« plusieurs bugs de cache-busting trouvés » au gate build + incident propagation CDN/Service Worker du 2026-07-18, voir `bugs.md`). Défaut « en cas de doute → risque élevé » applicable de toute façon.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation : **reconnaissance statique read-only (sans modifier le code) pour rendre l'escalade actionnable.**
  1. **État actuel** : `vite.config.ts` = uniquement `@hono/vite-build/cloudflare-pages` + `@hono/vite-dev-server`, aucune configuration de hachage. Les `public/static/js/*.js`/`*.css` sont servis tels quels (répertoire `public/`), hors du pipeline de hachage Vite.
  2. **Blast radius confirmé** : **29 fichiers HTML** dans `public/` référencent ces assets en dur via `<script src>`/`<link href>` (agenda, caisse, clients, dashboard, devis, factures, kanban, tickets, settings, stats, stock, sav, … + pages publiques). Hasher les noms sans mettre à jour ces 29 pages **ET** la liste de précache `APP_SHELL` de `public/sw.js` → 404 sur chaque page (nom d'asset introuvable). La sous-tâche « config Vite » n'est donc **pas isolable** : elle entraîne mécaniquement les 4 autres sous-tâches du chantier (manifeste, réécriture des refs HTML, régénération `APP_SHELL`, en-têtes `Cache-Control immutable`). >> 8 fichiers → périmètre architectural.
  3. **Pourquoi l'escalade plutôt que l'auto-fix** : `loop-policy.md` interdit l'auto-commit sur périmètre large/architectural. De plus cette zone touche directement le Service Worker et le cache-busting, dont un bug expédie du contenu périmé/cassé aux utilisateurs en prod (classe d'incident déjà survenue le 2026-07-18) — exactement le risque que la politique protège.
  4. **Recommandation** : traiter ce chantier par le **pipeline superpowers complet** en session humaine (`brainstorming` → `writing-plans` dans `docs/superpowers/plans/` → `subagent-driven-development`), pas via la loop autonome. Décisions de design à trancher côté humain avant tout code : (a) intégrer `public/static/js/*` au pipeline Vite (input `rollupOptions`) vs plugin de post-build qui hashe + réécrit ; (b) format du manifeste (nom logique → nom hashé) et point de consommation (injection build-time dans les 29 HTML vs résolution runtime) ; (c) régénération de `APP_SHELL` de `sw.js` depuis le manifeste + politique network-first sur `sw.js`/HTML d'entrée. Les 6 sous-tâches du chantier (todo.md:13-18) forment un tout cohérent à planifier ensemble, pas tâche par tâche via la loop. Après implémentation : `npm run build` + gate Playwright + validation live (`wrangler pages dev`) obligatoires, et incrément `CACHE_VERSION` sur la dernière tâche frontend (règle CLAUDE.md).

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche de tête : relire + merger + déployer le fix `fix/isolation-tickets-put-delete`

- Tâche : « Relire le diff (`git diff main fix/isolation-tickets-put-delete`), merger sur `main`, puis déployer (`npm run build && wrangler pages deploy`) » — `project-docs/todo.md:15` (section 🔴 PRIORITÉ CRITIQUE — Faille isolation `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`). Tête de file selon l'ordre déterministe (priorité 🔴 d'abord) : première tâche non cochée d'une section 🔴. Première sélection de CETTE tâche (les runs précédents avaient escaladé les tâches sœurs `GET /:id`, audit `PUT/DELETE/acompte`, cache-busting) → pas de `--skip`, traitée normalement.
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — deux déclencheurs indépendants : (1) **isolation multi-tenant** — le diff à merger est *entièrement* une correction de sécurité `boutique_id` sur 3 routes d'écriture de tickets (`loop-policy.md` § Classification, catégorie interdite à l'auto-commit) ; (2) la tâche exige explicitement `wrangler pages deploy` — **garde-fou absolu de `loop-policy.md` : la loop ne déploie JAMAIS en prod, aucune exception**, le déploiement reste un geste humain. La relecture d'un diff de sécurité avant merge est elle-même une décision humaine.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Blocage classification (bloquant, primaire)** : tâche à risque élevé (isolation multi-tenant) **et** exigeant un `wrangler pages deploy` que la loop n'a jamais le droit de faire. Escalade obligatoire, indépendamment de tout le reste. La branche `fix/isolation-tickets-put-delete` (fix préparé par la loop-engineering, run antérieur, non mergé) attend une **relecture humaine du diff + merge + déploiement manuel** — exactement le geste humain que la politique réserve.
  2. **Blocage environnement (transversal, hérité des runs précédents)** : session non-interactive ; l'exécution de `node`/`npm`/`npx` est soumise à approbation interactive impossible à accorder ici (vérifié ce run : `node --version` et `git` passent, mais `node scripts/loop/pick-task.mjs`, `node scripts/loop/check-quota.mjs`, `npx vitest`, `npx ccusage` sont tous refusés, avec et sans désactivation du sandbox). Conséquences : Étape 0bis (gate quota `check-quota.mjs`) inexécutable → traitée en **fail-open** (`loop-policy.md` : ne jamais bloquer sur une estimation quota absente) mais signalée ici ; Étape 5 (gates `vitest`/`tsc`/`build`/Playwright) inexécutable → aucun auto-commit possible même sur une tâche à risque faible. La loop-engineering n'est réellement opérante que lancée via `scripts/loop/run-loop.ps1` dans une session CLI locale normale (permissions node/npm accordées), pas dans une session restreinte non-interactive.
  3. **Blocage outillage (hérité, non résolu)** : `pick-task.mjs` reste cassé sur Windows (CRLF, `.split('\n')` en `pick-task.mjs:40` — voir run « sélecteur de tâches cassé » ci-dessus). Le correctif d'une ligne (`.split(/\r?\n/)`) a été proposé mais **jamais validé/appliqué par l'utilisateur** ; il attend une décision humaine (règle « proposer avant de modifier ») et ne doit pas être re-tenté en boucle. Sélection de la tâche de tête faite manuellement par lecture de `todo.md` (méthode identique aux runs précédents), aboutissant au même résultat déterministe.
  - **Recommandation (actionnable, côté humain)** :
    (a) **Fix isolation `PUT/DELETE`** : relire `git diff main fix/isolation-tickets-put-delete` (doit appliquer le patron `getTicketById` + `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403` déjà validé en prod par `ae6795f`, sur les 3 routes `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`), vérifier que `tests/e2e/isolation.spec.ts` couvre bien ces 3 routes (10/10 annoncés dans todo.md:14), lancer localement `npx vitest run` + `npx tsc --noEmit` + `npm run build` + `npm run test:e2e`, puis `git checkout main && git merge --ff-only fix/isolation-tickets-put-delete && git push origin main`, et **déployer manuellement** `npm run build && wrangler pages deploy dist --project-name izigsm` (confirmation explicite requise). Cocher `todo.md:15` + ajouter un checkpoint `current-state.md` après validation prod (`repairdesk.fr/api/health` → 200).
    (b) **Débloquer la loop** : appliquer le correctif CRLF `pick-task.mjs:40` (`.split(/\r?\n/)`) et relancer la loop via `run-loop.ps1` en session CLI locale normale, sinon chaque run reste inopérant/escaladé pour cause d'environnement.
