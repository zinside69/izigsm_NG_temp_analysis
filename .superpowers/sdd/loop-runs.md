# Ledger de la loop d'ingénierie autonome (loop-engineering)

_Append-only. Une entrée par exécution du skill `.claude/skills/loop-engineering/SKILL.md`.
Jamais réécrit — nouvelle entrée ajoutée en dessous. Voir `project-docs/loop-policy.md`._

_⚠ Chaque entrée est un instantané figé de ce qui était vrai **au moment de ce run
précis** (bug non corrigé, blocage encore présent, etc.) — pas une trace vivante mise
à jour. Un blocage mentionné dans une entrée ancienne peut être résolu depuis. Pour
l'état **actuel** d'un bug ou d'une tâche, se référer à `project-docs/bugs.md` et
`project-docs/todo.md`, jamais à ce fichier seul._

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche de tête : faille isolation `GET /api/tickets/:id`

- Tâche : « Corriger `GET /api/tickets/:id` (`src/routes/tickets.ts:160`) avec le patron `getBoutiqueId(...)` + `403` » — `project-docs/todo.md:5` (section 🔴 PRIORITÉ CRITIQUE). Tête de file selon l'ordre déterministe de `pick-task.mjs` (priorité 🔴 d'abord).
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code.
- Risque : **élevé** (isolation multi-tenant — `boutique_id` / `getBoutiqueId`). La tâche est elle-même documentée dans `todo.md` comme « classé risque élevé par `loop-policy.md` — escaladé, pas d'auto-fix ». `loop-policy.md` § Classification interdit l'auto-commit sur cette catégorie.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — voir Détail).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Blocage environnement (bloquant, indépendant du choix de tâche)** : cette session est non-interactive et l'exécution de `node`/`npm`/`npx` est soumise à approbation interactive impossible à accorder ici. `scripts/loop/check-quota.mjs` (Étape 0bis, gate quota obligatoire *avant tout le reste*) n'a pas pu être exécuté — testé via Bash et PowerShell, avec et sans sandbox, systématiquement refusé. Les gates de l'Étape 5 (`vitest`, `tsc --noEmit`, `npm run build`, Playwright) sont tous des invocations `node`/`npm` et sont donc également inexécutables. La machinerie de sûreté de la loop ne peut pas tourner → per garde-fou global « en cas de doute non couvert → escalader », arrêt sans implémentation.
  2. **Blocage classification (bloquant, indépendant du blocage environnement)** : la tâche de tête est à risque élevé (isolation multi-tenant) et déjà escaladée sans décision humaine actée depuis (aucune entrée `decisions.md` ni changement du texte de la tâche). `loop-policy.md` interdit l'auto-fix ; l'Étape 1 impose de ne jamais re-tenter en boucle une telle tâche.
  - Recommandation : (a) pour lever le blocage environnement, lancer la loop via `scripts/loop/run-loop.ps1` dans une session CLI locale normale (permissions node/npm accordées), pas dans une session non-interactive restreinte ; (b) pour la faille isolation elle-même, c'est une correction de sécurité qui nécessite une décision humaine explicite (voir `bugs.md` § faille `GET /api/tickets/:id`) — appliquer manuellement le patron `getBoutiqueId(user, queryBoutiqueId)` + `ticket.boutique_id !== boutiqueId → 403` déjà utilisé sur `/api/tickets/:id/photos`, puis auditer `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`, `POST /:id/acompte`.

## Run 2026-07-19 (heure exacte indisponible — non-interactif) — sélecteur de tâches cassé (faux « backlog vide »)

- Tâche : **aucune tâche sélectionnée** — `pick-task.mjs` retourne `{"empty":true}` ET `--all` retourne `[]`, alors que `project-docs/todo.md` contient **54 tâches non cochées** (`grep -c '- [ ]'`), dont la section 🔴 PRIORITÉ CRITIQUE et plusieurs items 🔴.
- Issue : **escaladé** — aucun commit, aucun worktree, aucune modification de code. Le run n'a PAS traité de tâche produit, mais le résultat « empty » est un **faux positif** (bug d'outillage), pas un vrai backlog vide → ne pas appliquer la branche « empty → stop » du SKILL comme si le backlog était réellement épuisé.
- Risque : **faible** (le correctif proposé touche `scripts/loop/pick-task.mjs`, outillage de la loop — hors catégories de risque produit de `loop-policy.md` : ni auth, ni multi-tenant, ni NF525, ni RGPD, ni migration).
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — arrêt à l'étape 1, sélection impossible).
- Worktree : aucun (arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Cause racine confirmée** : `git ls-files --eol` montre le working tree en **CRLF** (`w/crlf`) pour `project-docs/todo.md`, `docs/TODO.md` ET `scripts/loop/pick-task.mjs` (checkout Windows, `core.autocrlf`, aucun `.gitattributes`). `pick-task.mjs:40` fait `readFileSync(relPath,'utf8').split('\n')` : chaque ligne conserve un `\r` traînant. Les regex `^(#{2,4})\s+(.*)$` (titres) et `^(\s*)-\s*\[ \]\s+(.*)$` (tâches) échouent car `.` ne consomme pas `\r` et `$` (sans flag `m`) ne matche pas avant `\r`. → 0 tâche parsée sur Windows, à chaque run. La loop se croit à vide et ne fait jamais rien.
  2. **Impact** : sur toute machine Windows (le poste principal de l'utilisateur, cf. CLAUDE.md), la loop-engineering est **silencieusement inopérante** — elle rapporte « backlog vide » alors que le backlog est plein (54 tâches, dont des 🔴). Un run précédent a d'ailleurs pu masquer cet état.
  3. **Correctif proposé (non appliqué — attente validation utilisateur, règle « proposer avant de modifier »)** : `pick-task.mjs:40` remplacer `.split('\n')` par `.split(/\r?\n/)`. Une ligne, robuste LF et CRLF. Vérification suggérée : `node scripts/loop/pick-task.mjs --all` doit alors lister les 54 tâches (tête de file = la 🔴 critique restante).
  4. Ce correctif étant un changement d'outillage à faible risque, il serait éligible à l'auto-commit de la loop une fois validé — mais il conditionne la loop elle-même, d'où l'escalade explicite plutôt qu'un auto-fix silencieux.

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche `cac47a6824` : audit isolation `PUT /:id` · `PUT /:id/statut` · `DELETE /:id` · `POST /:id/acompte`

- Tâche : « Auditer dans la foulée `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`, `POST /:id/acompte` (même service, même risque potentiel, pas encore vérifiés un par un) » — `project-docs/todo.md:6` (section 🔴 PRIORITÉ CRITIQUE, suite du fix `GET /api/tickets/:id`). Tête de file selon `pick-task.mjs`. Première sélection de CETTE tâche (le run précédent avait escaladé la tâche sœur `GET /:id`, désormais corrigée `ae6795f`) → pas de `--skip`, traitée normalement.
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — double catégorie `loop-policy.md` : isolation multi-tenant (`boutique_id` / `getBoutiqueId`) **ET** paiement / acompte. `loop-policy.md` § Classification interdit l'auto-commit sur ces catégories ; § Protocole d'escalade impose un rapport écrit.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation : **audit statique read-only effectué (sans modifier le code) pour rendre l'escalade actionnable.** Résultat — **3 des 4 endpoints sont vulnérables**, même classe que la faille `GET /:id` corrigée le même jour :
  1. **`POST /api/tickets/:id/acompte`** (`src/routes/tickets.ts:575`) → **SÛR**. Contient déjà `getTicketById` + `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403` (lignes 581-583). Aucune action.
  2. **`PUT /api/tickets/:id`** (`src/routes/tickets.ts:259`) → **VULNÉRABLE**. Aucun garde d'isolation ; passe `user.sub` seul à `updateTicket` (`src/services/ticketService.ts:552`) dont le `SELECT`/`UPDATE` filtre uniquement par `id` (aucun `boutique_id`). N'importe quel compte authentifié peut modifier un ticket d'une autre boutique (diagnostic, prix, notes, signature…) en itérant sur l'ID.
  3. **`PUT /api/tickets/:id/statut`** (`src/routes/tickets.ts:287`) → **VULNÉRABLE**. Idem : `updateStatutTicket` (`ticketService.ts:625`) filtre par `id` seul. Cross-boutique en écriture, et déclenche en plus la création de garantie + emails client (`sendTicketTermine`/`sendTicketLivre`) sur un ticket d'une autre boutique.
  4. **`DELETE /api/tickets/:id`** (`src/routes/tickets.ts:352`) → **VULNÉRABLE**. `requireRole('admin','manager')` limite le rôle mais **pas la boutique** ; `deleteTicket` (`ticketService.ts:688`) fait un soft-delete filtré par `id` seul. Un admin/manager d'une boutique peut soft-delete le ticket d'une autre boutique.
  - **Recommandation** : correction de sécurité à faire **manuellement par l'utilisateur** (décision humaine explicite, hors périmètre auto-fix de la loop). Deux patrons possibles, à trancher côté humain : (A) réappliquer dans chaque route le garde déjà utilisé sur `GET /:id`, `/:id/photos` et `/:id/acompte` (`getTicketById`/`getTicketForPhoto` → `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403`) avant d'appeler le service ; **ou** (B) durcir la couche service (`updateTicket`/`updateStatutTicket`/`deleteTicket`) pour recevoir et filtrer par `boutique_id` (défense en profondeur, cohérent avec l'invariant « ne jamais supposer qu'un filtre en amont suffit » de `CLAUDE.md`). Le patron (A) est le plus proche du fix `ae6795f` déjà validé en prod. Après correction : couvrir les 3 endpoints par `tests/e2e/isolation.spec.ts` (même gate qui a révélé la faille `GET /:id`) et documenter dans `bugs.md`.

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche `61fc30924e` : cache-busting par hash de contenu (config Vite)

- Tâche : « Configurer Vite pour hasher les assets `public/static/js/*.js`/`*.css` (actuellement copiés tels quels, hors du pipeline de build Vite qui gère déjà le hachage pour d'autres assets) » — `project-docs/todo.md:13` (section 🔴 PRIORITÉ prochaine session — Cache-busting par hash de contenu). Sélectionnée après `--skip cac47a6824` (tâche de tête `cac47a6824` audit isolation `PUT/DELETE/acompte` : déjà escaladée au run précédent, risque élevé, aucune décision humaine actée depuis → passée par `pick-task.mjs --skip` conformément à l'Étape 1 du SKILL, non re-tentée).
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — deux déclencheurs indépendants de `loop-policy.md` § Classification : (1) **Périmètre large / architectural** — changement de plus de ~8 fichiers ; (2) zone cache-busting historiquement fragile (« plusieurs bugs de cache-busting trouvés » au gate build + incident propagation CDN/Service Worker du 2026-07-18, voir `bugs.md`). Défaut « en cas de doute → risque élevé » applicable de toute façon.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation : **reconnaissance statique read-only (sans modifier le code) pour rendre l'escalade actionnable.**
  1. **État actuel** : `vite.config.ts` = uniquement `@hono/vite-build/cloudflare-pages` + `@hono/vite-dev-server`, aucune configuration de hachage. Les `public/static/js/*.js`/`*.css` sont servis tels quels (répertoire `public/`), hors du pipeline de hachage Vite.
  2. **Blast radius confirmé** : **29 fichiers HTML** dans `public/` référencent ces assets en dur via `<script src>`/`<link href>` (agenda, caisse, clients, dashboard, devis, factures, kanban, tickets, settings, stats, stock, sav, … + pages publiques). Hasher les noms sans mettre à jour ces 29 pages **ET** la liste de précache `APP_SHELL` de `public/sw.js` → 404 sur chaque page (nom d'asset introuvable). La sous-tâche « config Vite » n'est donc **pas isolable** : elle entraîne mécaniquement les 4 autres sous-tâches du chantier (manifeste, réécriture des refs HTML, régénération `APP_SHELL`, en-têtes `Cache-Control immutable`). >> 8 fichiers → périmètre architectural.
  3. **Pourquoi l'escalade plutôt que l'auto-fix** : `loop-policy.md` interdit l'auto-commit sur périmètre large/architectural. De plus cette zone touche directement le Service Worker et le cache-busting, dont un bug expédie du contenu périmé/cassé aux utilisateurs en prod (classe d'incident déjà survenue le 2026-07-18) — exactement le risque que la politique protège.
  4. **Recommandation** : traiter ce chantier par le **pipeline superpowers complet** en session humaine (`brainstorming` → `writing-plans` dans `docs/superpowers/plans/` → `subagent-driven-development`), pas via la loop autonome. Décisions de design à trancher côté humain avant tout code : (a) intégrer `public/static/js/*` au pipeline Vite (input `rollupOptions`) vs plugin de post-build qui hashe + réécrit ; (b) format du manifeste (nom logique → nom hashé) et point de consommation (injection build-time dans les 29 HTML vs résolution runtime) ; (c) régénération de `APP_SHELL` de `sw.js` depuis le manifeste + politique network-first sur `sw.js`/HTML d'entrée. Les 6 sous-tâches du chantier (todo.md:13-18) forment un tout cohérent à planifier ensemble, pas tâche par tâche via la loop. Après implémentation : `npm run build` + gate Playwright + validation live (`wrangler pages dev`) obligatoires, et incrément `CACHE_VERSION` sur la dernière tâche frontend (règle CLAUDE.md).

## Run 2026-07-19 (heure indisponible — session non-interactive) — tâche de tête : relire + merger + déployer le fix `fix/isolation-tickets-put-delete`

- Tâche : « Relire le diff (`git diff main fix/isolation-tickets-put-delete`), merger sur `main`, puis déployer (`npm run build && wrangler pages deploy`) » — `project-docs/todo.md:15` (section 🔴 PRIORITÉ CRITIQUE — Faille isolation `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`). Tête de file selon l'ordre déterministe (priorité 🔴 d'abord) : première tâche non cochée d'une section 🔴. Première sélection de CETTE tâche (les runs précédents avaient escaladé les tâches sœurs `GET /:id`, audit `PUT/DELETE/acompte`, cache-busting) → pas de `--skip`, traitée normalement.
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — deux déclencheurs indépendants : (1) **isolation multi-tenant** — le diff à merger est *entièrement* une correction de sécurité `boutique_id` sur 3 routes d'écriture de tickets (`loop-policy.md` § Classification, catégorie interdite à l'auto-commit) ; (2) la tâche exige explicitement `wrangler pages deploy` — **garde-fou absolu de `loop-policy.md` : la loop ne déploie JAMAIS en prod, aucune exception**, le déploiement reste un geste humain. La relecture d'un diff de sécurité avant merge est elle-même une décision humaine.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Blocage classification (bloquant, primaire)** : tâche à risque élevé (isolation multi-tenant) **et** exigeant un `wrangler pages deploy` que la loop n'a jamais le droit de faire. Escalade obligatoire, indépendamment de tout le reste. La branche `fix/isolation-tickets-put-delete` (fix préparé par la loop-engineering, run antérieur, non mergé) attend une **relecture humaine du diff + merge + déploiement manuel** — exactement le geste humain que la politique réserve.
  2. **Blocage environnement (transversal, hérité des runs précédents)** : session non-interactive ; l'exécution de `node`/`npm`/`npx` est soumise à approbation interactive impossible à accorder ici (vérifié ce run : `node --version` et `git` passent, mais `node scripts/loop/pick-task.mjs`, `node scripts/loop/check-quota.mjs`, `npx vitest`, `npx ccusage` sont tous refusés, avec et sans désactivation du sandbox). Conséquences : Étape 0bis (gate quota `check-quota.mjs`) inexécutable → traitée en **fail-open** (`loop-policy.md` : ne jamais bloquer sur une estimation quota absente) mais signalée ici ; Étape 5 (gates `vitest`/`tsc`/`build`/Playwright) inexécutable → aucun auto-commit possible même sur une tâche à risque faible. La loop-engineering n'est réellement opérante que lancée via `scripts/loop/run-loop.ps1` dans une session CLI locale normale (permissions node/npm accordées), pas dans une session restreinte non-interactive.
  3. **Blocage outillage (hérité, non résolu)** : `pick-task.mjs` reste cassé sur Windows (CRLF, `.split('\n')` en `pick-task.mjs:40` — voir run « sélecteur de tâches cassé » ci-dessus). Le correctif d'une ligne (`.split(/\r?\n/)`) a été proposé mais **jamais validé/appliqué par l'utilisateur** ; il attend une décision humaine (règle « proposer avant de modifier ») et ne doit pas être re-tenté en boucle. Sélection de la tâche de tête faite manuellement par lecture de `todo.md` (méthode identique aux runs précédents), aboutissant au même résultat déterministe.
  - **Recommandation (actionnable, côté humain)** :
    (a) **Fix isolation `PUT/DELETE`** : relire `git diff main fix/isolation-tickets-put-delete` (doit appliquer le patron `getTicketById` + `if (user.role !== 'admin' && ticket.boutique_id !== user.boutique_id) → 403` déjà validé en prod par `ae6795f`, sur les 3 routes `PUT /:id`, `PUT /:id/statut`, `DELETE /:id`), vérifier que `tests/e2e/isolation.spec.ts` couvre bien ces 3 routes (10/10 annoncés dans todo.md:14), lancer localement `npx vitest run` + `npx tsc --noEmit` + `npm run build` + `npm run test:e2e`, puis `git checkout main && git merge --ff-only fix/isolation-tickets-put-delete && git push origin main`, et **déployer manuellement** `npm run build && wrangler pages deploy dist --project-name izigsm` (confirmation explicite requise). Cocher `todo.md:15` + ajouter un checkpoint `current-state.md` après validation prod (`repairdesk.fr/api/health` → 200).
    (b) **Débloquer la loop** : appliquer le correctif CRLF `pick-task.mjs:40` (`.split(/\r?\n/)`) et relancer la loop via `run-loop.ps1` en session CLI locale normale, sinon chaque run reste inopérant/escaladé pour cause d'environnement.

## Run 2026-07-20 (heure indisponible — session non-interactive) — tâche de tête : deep-link admin (`GET /api/tickets`)

- Tâche : « Corriger le bug deep-link admin (voir `bugs.md`) — nécessite de modifier la route partagée `GET /api/tickets`, hors périmètre de Task 8, décision utilisateur : reporté à plus tard » — `project-docs/todo.md:36` (section « Chantier impression ticket »). Sélection manuelle (voir blocage outillage ci-dessous) : la seule section 🔴 avec des items ouverts est « Cache-busting par hash de contenu » — **déjà escaladée** au run `61fc30924e` (risque élevé architectural), texte de tâche inchangé et aucune décision actée dans `decisions.md` depuis → passée conformément à l'Étape 1 (règle `--skip`, ne jamais re-tenter en boucle). Les deux sections 🔴 « Faille isolation » sont désormais **entièrement `[x]`** (l'humain a traité les escalades des runs précédents : `ae6795f`, `22b3071`). Prochaine tâche non cochée en ordre déterministe après le skip → `todo.md:36`.
- Issue : **escaladé** — aucun commit, aucun worktree créé, aucune modification de code (arrêt à l'étape 2, classification).
- Risque : **élevé** — **isolation multi-tenant** : le correctif modifie la route partagée `GET /api/tickets`, qui exige aujourd'hui un `boutique_id` sans exception admin ; introduire un chemin pour `admin` (`boutique_id: null`) touche directement la logique de dérivation/filtrage `boutique_id` — catégorie interdite à l'auto-commit par `loop-policy.md` § Classification (historique de 3 failles réelles sur ce point). De plus la tâche porte une **décision humaine explicite de report** (« reporté à plus tard ») → hors périmètre auto-fix de la loop de toute façon.
- Gates : vitest n·a · tsc n·a · build n·a · playwright n·a · browser-use n·a (aucun gate exécuté — escalade sur classification, avant l'étape 3/worktree ; **et** gates matériellement inexécutables ce run, voir blocage environnement).
- Worktree : aucun (rien créé — arrêt avant l'étape 3).
- Détail / recommandation :
  1. **Blocage environnement (transversal, terminal pour ce run, hérité des runs précédents et reconfirmé)** : session non-interactive. Vérifié empiriquement ce run — `node --version` (v24.15.0) et `git` **passent** (allowlist), mais `node scripts/loop/check-quota.mjs`, `node scripts/loop/pick-task.mjs` et `npx --yes ccusage@latest …` sont **tous refusés** (approbation interactive impossible). Conséquences : Étape 0bis (gate quota) inexécutable → traitée **fail-open** (`loop-policy.md` : ne jamais bloquer sur une estimation quota absente), signalée ici ; Étape 5 (`vitest`/`tsc`/`build`/Playwright/browser-use, toutes des invocations `npx`/`npm`) inexécutable → **aucun auto-commit possible sur AUCUNE tâche ce run, même à risque faible** (L2 exige tous les gates verts). La loop n'est réellement opérante que lancée via `scripts/loop/run-loop.ps1` dans une session CLI locale normale.
  2. **Blocage classification (bloquant, primaire, indépendant du point 1)** : tâche à risque élevé (isolation multi-tenant) et déjà parquée par décision humaine — escalade obligatoire quoi qu'il arrive.
  3. **Blocage outillage (hérité, toujours non résolu)** : `pick-task.mjs` reste cassé sur Windows (CRLF, `.split('\n')` en `pick-task.mjs:40`) — correctif d'une ligne proposé (`.split(/\r?\n/)`) mais **jamais validé/appliqué par l'utilisateur** (règle « proposer avant de modifier »), non re-tenté en boucle. Sélection de tâche faite manuellement par lecture de `todo.md`, aboutissant au même résultat déterministe.
  - **Recommandation (actionnable, côté humain)** :
    (a) **Débloquer la loop en priorité** — c'est le vrai goulot : appliquer le correctif CRLF `pick-task.mjs:40` (`.split(/\r?\n/)`) puis relancer via `scripts/loop/run-loop.ps1` dans une session CLI locale normale (permissions `node`/`npm` accordées). Sans cela, chaque run reste inopérant/escaladé pour cause d'environnement, quelle que soit la tâche.
    (b) **Bug deep-link admin lui-même** (risque élevé, décision humaine requise) : introduire une exception admin sur `GET /api/tickets` (`admin` = `boutique_id: null` → autoriser sans filtre, ou exiger un `boutique_id` explicite en query) **est une modification de l'isolation multi-tenant** ; à traiter manuellement avec revue sécurité + extension de `tests/e2e/isolation.spec.ts` (même gate qui a révélé les failles `GET/PUT/DELETE /:id`), pas via la loop autonome.
    (c) **Où la loop serait réellement productive une fois débloquée** : la première tâche véritablement **éligible auto-commit** (risque faible explicite selon `loop-policy.md` § « Risque faible ») en aval du backlog est l'audit du pattern `r.success`/`r.data` sur `agenda.js`/`sav.js`/`stats.html` (`todo.md` § « Bug étendu — pattern `r.success`/`r.data` », ~ligne 44) — frontend isolé, aucune catégorie sensible. C'est le premier candidat que la loop devrait traiter en session locale normale (gates exécutables), en validant chaque site en live avec de vraies données (un affichage à zéro/vide ne prouve rien pour cette classe de bug silencieux).

## Run 2026-07-20 — tâche `eabf928d00` : audit `agenda.js` (pattern `r.success`/`r.data`) → déjà corrigé, réconciliation backlog

- Tâche : « Auditer `agenda.js` fonction par fonction (KPIs, RDV, clients/tickets cache, détail RDV) » — `project-docs/todo.md:44` (section « Bug étendu — pattern `r.success`/`r.data` »). Sélectionnée en ordre déterministe **après skips Étape 1** : chantier cache-busting (6 items, 🔴) déjà escaladé run `61fc30924e` (périmètre architectural), aucune décision actée depuis → passé ; deep-link admin déjà escaladé run 2026-07-20 (« reporté à plus tard », risque élevé isolation) → passé ; L37 (décision produit), L38 (convention nommage), L40 (déploiement — la loop ne déploie jamais), L39 (nom boutique sur fiche imprimée — touche sélection boutique → risque élevé par défaut) écartés. `eabf928d00` = première tâche implémentable **de risque faible explicite** (`loop-policy.md` § « Risque faible » : bugs d'affichage/frontend, pattern r.success/r.data) — exactement le candidat recommandé par le run précédent (rec. (c)).
- Issue : **commit `d13976c`** (réconciliation `todo.md` + `current-state.md`) + ce ledger — réconciliation documentaire (aucun changement de code). L'audit conclut que la tâche (et ses sœurs L45 `sav.js`, L46 `stats.html`, L47/L48 méthodo) est **déjà résolue** par le commit `c281411` (2026-07-17, « fix(agenda,sav,stats): pattern r.success/r.data cassé sur 19 fonctions »), validé en local live à l'époque. Seules les checkboxes `todo.md` étaient restées décochées (décalage documentation/code, cf. bug slug `92f0db8`). Audit statique loop confirmé : plus aucun `r.success`/`r.data` direct fautif dans les 3 fichiers, uniquement `r.ok`/`r.data?.data`/`r.error` + commentaires.
- Risque : **faible** — réconciliation documentaire uniquement (`todo.md` + `current-state.md` + ce ledger), zéro delta de code produit. Le correctif sous-jacent (`c281411`) était déjà mergé et validé en live.
- Gates : vitest ✅ (824/826, 2 échecs fuseau horaire pré-existants `computeFin()`/`updateRendezVous` inchangés — baseline stable) · tsc ✅ (aucune nouvelle erreur ; seules les pré-existantes sur anciens fichiers services/tests e2e) · build ✅ (`vite build` OK, 1.18s) · playwright n·a (zéro delta code) · browser-use n·a (zéro delta code ; live-validation déjà faite dans `c281411`).
- Worktree : créé (`../izigsm-loop-reconcile-rsuccess`, branche `loop/reconcile-rsuccess-audit`) puis **retiré** — chemin hors du périmètre de permissions en écriture de cette session (échec Edit). Réconciliation faite sur le checkout principal `main` (docs seulement, aucun risque de build ; cohérent avec les commits ledger déjà présents sur `main`). Déviation environnement notée pour transparence.
- Détail / recommandation : **environnement de cette session opérant** (contrairement aux runs non-interactifs précédents) — `node`/`npm`/`npx` exécutables, `pick-task.mjs` fonctionnel (le correctif CRLF `.split(/\r?\n/)` a bien été appliqué en `pick-task.mjs:43`), gate quota `check-quota.mjs` renvoie code 2 (historique local insuffisant → **fail-open** conformément à `loop-policy.md`, signalé). Prochaine tâche réellement implémentable et de risque faible pour un futur run : aucune évidente dans le haut du backlog (le reste des 🔴 est escaladé/parqué ; les items frontend r.success/r.data sont désormais tous cochés). Candidats de risque faible en aval : `tests/phoneCatalogService.test.ts` à créer (`todo.md`, § dette technique — « corrections de tests » = risque faible), et `docs/ARCHITECTURE_MODULES.md §2 obsolète` (documentation). À l'inverse, `escapeHtml()` manquant sur 5 templates email est **risque élevé** (§ Sécurité — échappement HTML) → escalade requise.

## Run 2026-07-20 — tâche `3a01544d33` : `docs/ARCHITECTURE_MODULES.md` §2 noms de tables obsolètes

- Tâche : « `docs/ARCHITECTURE_MODULES.md` §2 obsolète (noms de tables) » — `project-docs/todo.md:163` (§ « Dette technique découverte pendant la migration »). Sélectionnée en ordre déterministe **après skips/écartements Étape 1-2** : chantier cache-busting (🔴, 6 items) déjà escaladé run `61fc30924e` (architectural), aucune décision actée depuis → `--skip` ; L40 déploiement (la loop ne déploie jamais) ; deep-link admin déjà escaladé (isolation) ; L37/L135/L136 décisions produit ; L38 convention nommage ; L39 nom boutique fiche imprimée + L165 escapeHtml (risque élevé) ; L134 feature à construire — tous écartés. `3a01544d33` = première tâche **risque faible implémentable** (documentation) — exactement le candidat recommandé par le run précédent `eabf928d00`.
- Issue : **commit `2e6da16`** (`docs/ARCHITECTURE_MODULES.md` + `project-docs/todo.md` case cochée + `project-docs/current-state.md` checkpoint 41) + ce ledger. Correction de 5 noms de tables erronés dans le tableau §2, chacun vérifié contre les `CREATE TABLE` réels de `migrations/*.sql` : `statuts_historique`→`tickets_statuts_historique` (0004), `lignes_facture`→`lignes_document` (0006), `sessions_caisse`/`lignes_caisse`→`clotures_journalieres` (0008 — les deux premiers n'existent dans aucune migration), `otp_codes`→`otp_tokens` (0009), `tickets_sav`→`sav_dossiers` (0019). Portée limitée aux noms erronés (pas d'ajout de complétude `commissions`/`sequences`/migrations 0032-0036 — hors scope « noms obsolètes », évite le scope creep).
- Risque : **faible** — documentation seule (`docs/ARCHITECTURE_MODULES.md`), zéro delta de code produit, aucune catégorie sensible de `loop-policy.md` (ni auth, ni multi-tenant, ni NF525, ni RGPD, ni paiement, ni `migrations/*.sql`, ni sécurité). Éligible auto-commit L2.
- Gates : vitest ✅ (824/826, 2 échecs fuseau horaire pré-existants `computeFin()`/`updateRendezVous` inchangés — baseline stable) · tsc ✅ (0 nouvelle erreur ; markdown hors compilation TS → delta nul vs `main`, les erreurs restantes `servicesService.ts`/`stockService.ts`/`tests/e2e/*`/`setup.ts` sont pré-existantes) · build ✅ (`vite build` 1.93s) · playwright n·a (delta code nul) · browser-use n·a (delta code nul, pas de nouveau parcours utilisateur).
- Worktree : **aucun** (déviation Étape 3 assumée) — périmètre d'écriture de la session restreint à `izigsm/webapp`, worktree frère `../izigsm-loop-*` non-inscriptible (même contrainte que run `eabf928d00`). Changement docs-only à delta code nul → édité sur `main` directement, cohérent avec les commits docs sur `main` déjà présents (`d13976c`).
- Détail / recommandation :
  1. **Gate quota** (`check-quota.mjs`) → code 2 (historique local insuffisant) → **fail-open** conforme à `loop-policy.md`, signalé.
  2. **Anomalie à trancher côté humain** : `.claude/skills/loop-engineering/SKILL.md` est apparu modifié **en cours de run** (working tree propre à l'Étape 0 ET juste avant le commit), +6 lignes ajoutant un garde-fou « jamais de fichier de dump/debug laissé dans le repo ». Non causé par la tâche `3a01544d33` (churn harness/système de skill). **Ni commité ni reverté** (règle « proposer avant d'écraser ») — laissé tel quel dans le working tree, à valider/committer par l'utilisateur. ⚠ Tant qu'il reste non commité, il salit le tree et fera **escalader le prochain run planifié à l'Étape 0** (précondition tree propre) — décision utilisateur requise rapidement.
  3. Prochain candidat risque faible pour un futur run : `tests/phoneCatalogService.test.ts` à créer (`todo.md:168`, création de test = risque faible, gate vitest direct). Le reste du haut de backlog reste escaladé/parqué (risque élevé).

## Run 2026-07-23 08:56 (+0200) — tâche `96222a31f3` : `tests/phoneCatalogService.test.ts` à créer

- Tâche : « `tests/phoneCatalogService.test.ts` à créer » — `project-docs/todo.md:168` (§ « Dette technique héritée »). Sélectionnée en ordre déterministe **après skips/écartements Étape 1-2** : chantier cache-busting (🔴, 6 items L22-27) déjà escaladé run `61fc30924e` (architectural), aucune décision actée depuis → vérifié (`decisions.md` sans entrée cache-busting/Vite, texte de tâche byte-identique à l'escalade) → `--skip` des 6 ids ; puis écartés L40 déploiement `[loop-safe]` (la loop ne déploie jamais — garde-fou absolu, le tag ne lève pas l'interdiction), deep-link admin L36 (isolation + reporté utilisateur), décisions produit L37/L135/L136, convention nommage L38, `nom boutique fiche imprimée` L39 (multi-tenant → doute → risque élevé), `escapeHtml` L165 (sécurité — échappement HTML → risque élevé), `assignation technicien` L134 (feature à construire), `3 tests fuseau horaire` L164 (baseline connue non-bloquante). `96222a31f3` = première tâche **risque faible implémentable** — candidat explicitement recommandé par les runs `eabf928d00` et `3a01544d33`.
- Issue : **commit `cb87767`** (`project-docs/todo.md` case L168 cochée + `project-docs/current-state.md` checkpoint 43 + ce ledger ; hash backfillé par un second commit docs, même pattern que run `3a01544d33` → `2e6da16`/`112d925`) — **réconciliation d'une case obsolète** : le fichier `tests/phoneCatalogService.test.ts` existe déjà (créé migration Ports & Adapters, checkpoint 14, 2026-07-15), 209 lignes / 11 tests couvrant les 5 fonctions exportées de `phoneCatalogService.ts` via `fetch` mocké en échec → repli dataset statique. Décalage documentation/code (même classe que case slug `92f0db8` / réconciliation r.success 2026-07-20). Aucun code produit modifié.
- Risque : **faible** — documentation seule (markdown : `todo.md`/`current-state.md`/ce ledger), zéro delta de code produit, aucune catégorie sensible `loop-policy.md`. Éligible auto-commit L2.
- Gates : vitest ✅ (824/826, 2 échecs fuseau horaire pré-existants `computeFin()`/`updateRendezVous` inchangés — baseline stable ; suite ciblée `phoneCatalogService.test.ts` 11/11 vert) · tsc ✅ (0 nouvelle erreur ; delta nul vs `main`, erreurs restantes `servicesService.ts`/`stockService.ts`/`tests/e2e/*`/`setup.ts`/`stockService.test.ts` pré-existantes) · build ✅ (`vite build` 856ms) · playwright n·a (delta code nul) · browser-use n·a (delta code nul, pas de nouveau parcours utilisateur).
- Worktree : **aucun** (déviation Étape 3 assumée, cf. runs `eabf928d00`/`3a01544d33`) — périmètre d'écriture restreint à `izigsm/webapp`, worktree frère `../izigsm-loop-*` non-inscriptible. Changement docs-only à delta code nul → édité sur `main` directement.
- Détail / recommandation :
  1. **Gate quota** (`check-quota.mjs`) → code 2 (historique local insuffisant) → **fail-open** conforme à `loop-policy.md`, signalé.
  2. **Sous-item distinct laissé décoché** : `docs/TODO.md:225` (« Dette à traiter : `tests/phoneCatalogService.test.ts` manquant — 0 test sur ~1500 lignes ajoutées au fallback catalogue ») vise une exhaustivité de couverture du dataset de repli plus large que ce que le test actuel revendique — **non coché** (pas de sur-revendication : le test existe et couvre les 5 fonctions + le chemin de repli, mais pas une assertion ligne-à-ligne des ~1500 lignes statiques). À trancher côté humain si une couverture data-driven exhaustive est souhaitée.
  3. Prochain candidat risque faible pour un futur run : plus aucune tâche 🔴 implémentable non escaladée en tête ; en aval, la plupart des items restants sont des décisions produit, des features à construire, ou des catégories à risque élevé (sécurité `escapeHtml`, isolation, RGPD, paiement). Backlog risque-faible quasi épuisé — probable escalade « aucune tâche éligible » aux prochains runs tant qu'aucune nouvelle tâche loop-safe n'est ajoutée.

## Run 2026-07-23T10:14:07+02:00 — tâche `90109e0aad` : rebranding `app.js` « Mon Atelier » → « MyDesk »

- Tâche : « `public/static/js/app.js:27,385,386` — fallback `session.company`/`user.boutique_name` dans le wrapper ApiService partagé (impacte tout le dashboard) » — `project-docs/todo.md:225` (§ « Rebranding — retirer "Mon Atelier"/"monatelier", remplacer par "MyDesk" »). Sélectionnée en ordre déterministe **après skips/écartements Étape 1-2** : chantier cache-busting (🔴, 6 items L22-27) déjà escaladé run `61fc30924e` (architectural), aucune décision actée depuis (vérifié : `decisions.md` sans entrée cache-busting/Vite, texte byte-identique) → `--skip` des 6 ids ; puis écartés dans l'ordre L40 déploiement `[loop-safe]` (la loop ne déploie jamais — garde-fou absolu), deep-link admin L36 (isolation + reporté utilisateur), décisions produit L37/L135/L136, convention nommage L38, `nom boutique fiche imprimée` L39 (multi-tenant → doute → risque élevé), `assignation technicien` L134 (feature à construire, listing employés par boutique = multi-tenant), **`3 tests fuseau horaire` L164** (touche `computeFin`/logique DST de planification RDV + baseline stable dont dépend le gate vitest → pas un fix mécanique isolé, déféré), `escapeHtml` L165 (sécurité), `/robots.txt` Genspark L169 (obsolète — réconciliation de moindre valeur, déférée), `www 521` L170 (infra DNS Gandi, hors repo), RGPD L177, multi-sites L182, features marketing L186-189 (à construire), clarification agenda L207. `90109e0aad` = première tâche **risque faible réellement implémentable** (frontend, chaîne d'affichage).
- Issue : **escaladé** — travail complet et vérifié (gates 1-3 verts), **conservé sur branche `loop/rebrand-app-js-mydesk` (commit `94efe76`), NON mergé sur `main`**. Motif : le gate Playwright (Étape 5.4, condition dure L2 d'auto-commit dans `loop-policy.md`) est **inexécutable dans cet environnement Windows interactif**.
- Risque : **faible** — classification vérifiée sur le code réel (pas seulement le texte) : `app.js:27` (`session.company || 'Mon Atelier'`, libellé sidebar) + `app.js:424-425` (`user.boutique_name || 'Mon Atelier'` dans `storeSession`, champs d'affichage) — littéraux de repli d'affichage. Hors fichiers auth (pas `auth.ts`/`authService`/`lib/auth*`, ni JWT/OTP/OAuth), pas de `boutique_id`/`getBoutiqueId` (le champ isolant `boutique_id` L423 est intact), pas de SQL, pas de surface d'échappement HTML nouvelle (littéral→littéral), pas de migration. 2 fichiers code (`app.js` + `sw.js` bump `CACHE_VERSION` v2.65→v2.66). Éligible auto-commit L2 **sauf** gate Playwright.
- Gates : vitest ✅ (824/826, 2 échecs fuseau horaire pré-existants `agendaService.test.ts` `createRendezVous`/`updateRendezVous` — offset 2h UTC/Paris DST — inchangés en nombre et identité ; frontend non testé par vitest) · tsc ✅ (0 nouvelle erreur : `app.js`/`sw.js` hors compilation TS, delta nul ; erreurs restantes `factureService`/`photosService`/`rachatService`/`servicesService`/`stockService`/`tests/e2e/*`/`setup.ts`/`stockService.test.ts` pré-existantes) · build ✅ (`vite build` 958ms) · **playwright ❌ inexécutable** (`@playwright/test@^1.61.1` déclaré en devDependency mais NON provisionné dans `node_modules` ; `playwright.config.ts` cible en dur un chemin chromium Linux CI `/opt/pw-browsers/…linux/chrome` sans override Windows) · browser-use **n·a** (pas de nouveau parcours utilisateur — changement d'un libellé de repli d'affichage).
- Worktree : pas de worktree frère isolé (`../izigsm-loop-*` non-inscriptible, contrainte connue runs `eabf928d00`/`3a01544d33`/checkpoint 43) — **branche locale in-place `loop/rebrand-app-js-mydesk`** utilisée à la place (même périmètre inscriptible), **conservée** (escalade). `main` laissé propre.
- Détail / recommandation :
  1. **Gate quota** (`check-quota.mjs`) → code 2 (historique local insuffisant) → **fail-open** conforme `loop-policy.md`, signalé.
  2. **Constat durable (important pour les futurs runs)** : dans cet environnement, la loop **ne peut auto-commiter aucun vrai changement de code** — le gate e2e déterministe (condition dure L2) exige `@playwright/test` + un navigateur, non provisionnés, et la config vise un chemin Linux CI. Tous les runs *réussis* antérieurs étaient docs-only (« playwright n·a »). Provisionner à la volée (install harness + téléchargement navigateur ~150 Mo + override `PLAYWRIGHT_CHROMIUM_PATH` + `wrangler pages dev`/migrations D1) est disproportionné pour un changement de 3 chaînes et déclenche le garde-fou « doute non couvert → escalader ».
  3. **Recommandation actionnable (côté humain / CI)** : le diff est trivial et sûr (gates 1-3 verts ; un swap de littéral d'affichage ne peut pas affecter les specs e2e auth/health/isolation). Option A — merger après exécution du gate e2e dans l'environnement loop/CI provisionné (Linux) : `git checkout main && git merge --ff-only loop/rebrand-app-js-mydesk`, puis cocher `todo.md:225`. Option B — sur ce poste : `npm install` (provisionne `@playwright/test`) + `npx playwright install chromium` + `PLAYWRIGHT_CHROMIUM_PATH=<chromium windows>` + `npx wrangler d1 migrations apply DB --local` + `npx wrangler pages dev dist --local --port 3000` puis `npm run test:e2e`, et si vert, merger. Les 4 autres cases du chantier rebranding (`register.js` L226, `login.html` L227, `register.html` L228, **`auth.ts:652` L229 = fichier à risque élevé auth**, autres pages L230) restent décochées — tâches distinctes, l'item auth.ts nécessitera une attention particulière (fichier listé risque élevé, même si le diff n'est qu'un exemple JSDoc).

## Run 2026-07-23T11:11:24+02:00 — tâche `47f3736449` : réconciliation case obsolète `/robots.txt` 500 Genspark

- Tâche : « Investiguer `/robots.txt` 500 sur Genspark (sans objet une fois Genspark abandonné) » — `project-docs/todo.md:169` (§ « Dette technique héritée »). Sélectionnée en ordre déterministe après skips/écartements Étape 1-2 : chantier cache-busting L22-27 (6 ids, déjà escaladé run `61fc30924e` architectural, aucune décision actée depuis — `decisions.md` sans entrée, texte inchangé) → `--skip` ; puis L40 déploiement `[loop-safe]` (la loop ne déploie jamais), deep-link admin L36 (isolation + reporté utilisateur), décisions produit L37/L135/L136, convention nommage L38, `nom boutique fiche imprimée` L39 (multi-tenant → doute → risque élevé, code lu), `assignation technicien` L134 (feature à construire), `3 tests fuseau horaire` L164 (DST/baseline, non mécanique), `escapeHtml` L165 (sécurité XSS), **rebranding `app.js` L225 (`7af1ae2590`) — déjà escaladé au run 2026-07-23T10:14:07 (branche `loop/rebrand-app-js-mydesk` commit `94efe76` non mergée, gate Playwright inexécutable), aucune décision humaine depuis → `--skip`**. `47f3736449` = première tâche risque faible **complétable dans cet environnement** (docs-only ; les tâches code escaladent toutes sur le gate Playwright inexécutable, cf. run 10:14:07).
- Issue : **commité** (auto-commit L2, docs-only, dernier commit `docs:` du 2026-07-23 sur `main` — voir `git log`).
- Risque : **faible** — documentation seule (`todo.md`/`current-state.md`/ce ledger), zéro delta de code produit, aucune catégorie sensible `loop-policy.md`. Investigation : Genspark abandonné le 2026-07-10 (migration Cloudflare terminée) → 500 spécifique à cet hébergeur sans objet ; sur Cloudflare `/robots.txt` renvoie **200** (asset statique `public/robots.txt` exclu des Pages Functions par `public/_routes.json:11` + route Hono de redondance `src/index.tsx:242` `c.text(ROBOTS_TXT, 200, ...)`, réponse statique — aucun 500 possible).
- Gates : vitest ✅ (824/826, 2 échecs fuseau horaire pré-existants `agendaService.test.ts` `createRendezVous`/`updateRendezVous` inchangés — baseline stable) · tsc ✅ (0 nouvelle erreur ; markdown hors compilation TS, delta nul vs `main`) · build ✅ (`vite build` 1.12s) · playwright **n·a** (delta code nul) · browser-use **n·a** (pas de nouveau parcours utilisateur).
- Worktree : **aucun** (déviation Étape 3 assumée, cf. runs `eabf928d00`/`3a01544d33`/checkpoints 41/43) — périmètre d'écriture restreint à `izigsm/webapp`, worktree frère `../izigsm-loop-*` non-inscriptible. Changement docs-only à delta code nul → édité sur `main` directement.
- Détail / recommandation :
  1. **Gate quota** (`check-quota.mjs`) → code 2 (historique local insuffisant) → **fail-open** conforme `loop-policy.md`, signalé.
  2. **L170 laissée décochée** : `www.repairdesk.fr` → Error 521 est un problème d'infra DNS (service de redirection Gandi), hors du repo — non actionnable par la loop.
  3. **Backlog risque-faible docs-only épuisé** : après cette réconciliation, plus aucune case obsolète docs-only implémentable en tête. Les runs suivants devraient escalader « aucune tâche éligible » (ou traiter un changement de code qui escaladera sur le gate Playwright inexécutable) tant qu'aucune tâche loop-safe docs-only n'est ajoutée, OU que le gate e2e soit provisionné dans l'environnement loop/CI (cf. recommandation du run 10:14:07). Le rebranding `app.js` (branche `loop/rebrand-app-js-mydesk`, commit `94efe76`) reste prêt à merger dès que le gate e2e peut tourner.
